import { HighlightDirective } from './highlight.directive';

xdescribe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = null; // new HighlightDirective();
    expect(directive).toBeTruthy();
  });
});
